- name: hello
  hosts: all
  tasks:
    - name: Display all variables/facts known for a host
      debug:
        var: vars
    # - name: Print Vars
    #   debug:
    #     msg: "you have send the following event: '{{ansible_eda.event}}'"