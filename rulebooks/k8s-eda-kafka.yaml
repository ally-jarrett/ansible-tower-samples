---
- name: K8s Kafka Event
  hosts: localhost
  sources:
    - ansible.eda.kafka:
        host: automation-streams-kafka-bootstrap.skylight.svc.cluster.local
        port: 9092
        topic: kafka-test-topic
        group_id: ansible-eda

  rules:
    - name: kafka
      condition: event.body.request is defined and event.body.request == "test"
      action:
        run_job_template:
          name: EDA Hello World
          organization: Default